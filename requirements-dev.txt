# Development dependencies (not required at runtime)
-r requirements.txt

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Linting / formatting
ruff>=0.4.0
mypy>=1.10.0

# Build / packaging
pyinstaller>=6.6.0
build>=1.2.0
twine>=5.1.0
