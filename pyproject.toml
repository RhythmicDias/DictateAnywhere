[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "DictateAnywhere"
version = "2.0.1"
description = "Hybrid voice dictation for Windows — types anywhere using offline faster-whisper and Azure/Sarvam cloud backends"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
authors = [
    { name = "DictateAnywhere Contributors" }
]
keywords = ["dictation", "speech-to-text", "voice", "windows", "whisper", "azure", "sarvam", "ai"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Win32 (MS Windows)",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
]
dependencies = [
    "sounddevice>=0.5.1",
    "numpy>=1.26.4",
    "webrtcvad-wheels>=2.0.14",
    "faster-whisper>=1.0.3",
    "azure-cognitiveservices-speech>=1.37.0",
    "keyring>=25.2.1",
    "requests>=2.32.3",
    "websocket-client>=1.6.0",
]


[project.urls]
Homepage = "https://github.com/RhythmicDias/DictateAnywhere"
Repository = "https://github.com/RhythmicDias/DictateAnywhere"
"Bug Tracker" = "https://github.com/RhythmicDias/DictateAnywhere/issues"
Changelog = "https://github.com/RhythmicDias/DictateAnywhere/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
dictateanywhere = ["assets/icons/*"]
